# Agent Instructions

All project context, conventions, and instructions live in `CLAUDE.md`.

This file exists for tools that read `AGENTS.md` by convention. See `CLAUDE.md` for the authoritative source.
